# GOELRAH Configuration
# These are build-time defaults only. Users configure endpoint in the UI.
# No secrets here — this is a static site.

VITE_DEFAULT_ENDPOINT=http://localhost:11434
VITE_DEFAULT_MODEL=kimi-k2.5:cloud
